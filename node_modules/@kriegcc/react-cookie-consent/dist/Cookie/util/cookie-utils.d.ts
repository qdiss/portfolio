import { CookieCategories, CookieCategoryId, CookieStorage } from '../Cookies';
export type CookieData = {
    key: string;
    description: string;
    location: CookieStorage;
    duration: string;
};
export type CookieCategoryData = {
    id: CookieCategoryId;
    title: string;
    description: string;
    isMandatory: boolean;
    cookies: CookieData[];
};
export type CookiesFile = {
    categories: CookieCategoryData[];
};
export declare function validateCookiesJsonFile(data: any): data is CookiesFile;
export declare function loadCookies(cookiesFile: CookiesFile): CookieCategories;
