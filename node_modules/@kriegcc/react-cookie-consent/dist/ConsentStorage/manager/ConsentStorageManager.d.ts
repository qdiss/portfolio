import { StorageLocation } from '../types';
export type ConsentStatusValue = {
    location: StorageLocation;
    hasConsent: boolean;
};
export type ConsentStatus = Map<string, ConsentStatusValue>;
export declare class ConsentStorageManager {
    private consentStatus;
    constructor(initialConsentStatus: ConsentStatus);
    hasConsentFor(key: string, location: StorageLocation): boolean;
    isKeyKnown(key: string, location: StorageLocation): boolean;
    updateConsentStatus(consentStatus: ConsentStatus): void;
    private handleConsentRevocation;
    private deleteKeyInLocation;
}
